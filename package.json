{
  "name": "@volta-oss/micro-site-launchpad",
  "version": "3.0.0",
  "private": false,
  "type": "module",
  "description": "Opinionated one-week website tools for restaurants, salons, tutors, local services, and nonprofits.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test test/*.test.js",
    "start": "python3 -m http.server 4173",
    "validate": "npm test",
    "verify": "npm run build && npm run typecheck && node --test test/*.test.js && npm run certify",
    "certify": "npm run build && node --test test/v3.test.js",
    "test:all": "npm test",
    "clean": "rm -f src/*.js src/*.js.map src/*.d.ts test/*.js test/*.js.map test/*.d.ts",
    "ci": "npm ci && npm test && npm run test:backend --if-present",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "docker:build": "docker build -t volta-oss/micro-site-launchpad:local .",
    "docker:run": "docker compose up --build"
  },
  "keywords": [
    "digital presence",
    "digital-equity",
    "local-first",
    "oss",
    "release",
    "volta"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/volta-npo/micro-site-launchpad.git"
  },
  "homepage": "https://volta-npo.github.io/micro-site-launchpad/",
  "bugs": {
    "url": "https://github.com/volta-npo/micro-site-launchpad/issues"
  },
  "author": "Volta Open Source <volta.npo@gmail.com>",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "@types/node": "^26.1.0"
  }
}
